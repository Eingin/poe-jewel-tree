[package]
name = "poe-jewel-tree"
version = "0.1.0"
edition = "2021"
links = "foo"
build = "build.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cc = "1.0"